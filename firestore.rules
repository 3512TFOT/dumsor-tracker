rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Reports collection security
    match /reports/{reportId} {
      // Temporarily allow everything to check if rules are the bottleneck
      allow read, write: if true;
    }
  }
}
